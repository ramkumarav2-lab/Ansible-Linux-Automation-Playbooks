# Ansible-Linux-Automation-Playbooks
Tasks which includes checking the satellite, adding the entry of satellite to hosts file, Removing the old rpm's and installing new rpm's, register the server to satellite using the key as per the OS version and providing the output of latest available kernel version to proceed with patching activity.
